# 🚀 كيفية تشغيل لوحة التحكم الإدارية

## الخطوات السريعة

### 1. تأكد أن الخادم الخلفي يعمل

```bash
cd fraud_service
# إذا لم يكن يعمل، شغله:
nohup python3 -m uvicorn app.main:app --host 0.0.0.0 --port 8000 > server.log 2>&1 &
```

### 2. تثبيت وتشغيل React Dashboard

```bash
cd fraud_service/frontend/admin-dashboard

# تثبيت المكتبات (أول مرة فقط)
npm install

# تشغيل Dashboard
npm run dev
```

### 3. افتح المتصفح

افتح: **http://localhost:3000**

---

## 📋 المميزات

### ✅ عرض الزيارات
- وقت تسجيل الدخول لكل مستخدم
- Device ID (نفسه أم مختلف)
- طريقة المصادقة (QR Code vs Biometric)
- مستوى الخطر لكل زيارة

### ✅ الإحصائيات
- إجمالي الزيارات
- إجمالي المستخدمين
- الزيارات المشبوهة
- استخدام أجهزة متعددة

### ✅ الفلاتر
- البحث برقم الهوية
- البحث بالفرع
- فلترة بمستوى الخطر
- فلترة بطريقة المصادقة
- فلترة بالتاريخ

---

## 🎨 التصميم

- **ألوان أبشر**: الأخضر (#00A859)
- **واجهة عربية**: RTL support
- **تصميم حديث**: Responsive وواضح

---

## 🔧 استكشاف الأخطاء

### Dashboard لا يتصل بالخادم
```bash
# تحقق أن الخادم يعمل
curl http://localhost:8000/health
```

### خطأ في npm install
```bash
# تأكد من Node.js 18+
node --version

# امسح node_modules وأعد التثبيت
rm -rf node_modules package-lock.json
npm install
```

---

## 📱 الصفحات المتاحة

- **Dashboard الرئيسي**: http://localhost:3000
- **API Docs**: http://localhost:8000/docs
- **Health Check**: http://localhost:8000/health

